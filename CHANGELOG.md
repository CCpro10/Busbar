# 变更记录

## 0.4.1 — 2026-09-21

- 修复评测、训练多次读取文件导致来源哈希可能与实际数据不一致的问题；整份数据先校验，来源绑定捕获的字节。
- 选择头直接从已校验的权重/报告字节解码，避免校验后文件被替换；保持 0.4 artifact 格式兼容。
- JSON 报告及 manifest 原子发布且不覆盖已有文件；并发或失败写入不会留下可见的半份 JSON。
- 后端非法分数、计数与元数据统一报 `BackendOutputError`，HTTP 返回 502；请求错误仍为 422。仅含空白的候选描述明确拒绝。
- CLI 在加载模型前验证完整 run 输入及评测数据；候选编码器明确拒绝缺失或非法 EOS。
- 拆分训练计算与流程管理、数据与文件管理、测量与统计、CLI 分发、HTTP 路由和 Runtime 结果读取；CI 增加生产函数复杂度门禁。
- 修复包版本与 OpenAPI 版本漂移；从安装元数据读取版本。
- 重写项目首页和架构说明，补全接口、后端、评测及开发指南。详见 [验证记录](docs/quality-v041.md)。

## 0.4.0

引入原生 MLX 可训练候选选择头，与快捷词表读出共用决策 API；支持训练、验证选参、版本保存/加载、继续训练和回退。真实质量与速度对照见 [0.4 报告](docs/mac-v04.md)。

## 更早版本

[0.3 报告](docs/mac-v03.md) 记录较新小模型和跨项目对照；[0.2](docs/mac-v02.md) 记录 3B 与 vLLM Metal 接入；[0.1](docs/acceptance.md) 记录首次上下文复用与本机验收。
