# 开发指南

Busbar 的目标是让结构化决策具备清晰的接口、可验证的数值行为和可追溯的实验。改动前先阅读 [架构与不变量](docs/design.md)；选择最小且完整的实现，复用已有契约，避免把平台条件散入 Runtime。

## 环境

```bash
# 所有平台：普通契约测试、HTTP 测试、静态检查。
uv sync --locked --extra dev

# Apple Silicon：原生 MLX 和 HF 数值参考、真实训练/推理。
uv sync --locked --extra dev --extra mlx --extra hf
```

Python 3.11–3.13；CI 使用 3.13。依赖修改同时更新 `pyproject.toml` 与 `uv.lock`。vLLM Metal 必须使用 `scripts/setup-metal.sh` 创建的独立环境；不要把两个环境的 MLX/PyTorch 依赖混装。

下载权重放在 Hugging Face 缓存，训练头放在 `models/heads/`，未发布报告放在 `local-results/`，这些目录都不提交。公共结果经过内容检查后，按版本存入 `benchmarks/results/`，附复现步骤和 SHA256 清单。

## 代码与文件边界

- 函数说明意图、输入边界或平台差异；不重复翻译代码。公共接口和关键边界提供类型标注。
- 每个函数集中完成一种工作。数据读取/校验、模型计算、结果统计与持久化分离；不要只因函数长几行就制造一层转发。
- 文件按职责拆分。一个平台的专用逻辑留在后端；共享的验证、候选编码和版本格式只保留一份实现。
- 不捕获并隐藏未知异常，不把非法模型分数伪装成用户错误；不要静默截断、丢弃标签或回退到不同模型。
- 修改算法、编码或缓存时先说明不变量，再补用户场景回归。测试关注跨版本行为、输入输出契约与真实失败情形，避免仅复述函数实现。
- 增加后端或读出模式时，同时更新支持矩阵、失败行为、文档示例与真实验收证据。

生产源码通过 Ruff 的复杂度检查，上限 10；该限制不是质量的充分条件。拆分后应能从函数名和参数理解职责，而不是依赖全局可变状态。

## 提交前验证

```bash
uv run ruff check .
uv run ruff format --check .
uv run ruff check src --select C901 --config 'lint.mccabe.max-complexity=10'
uv run pytest -m 'not model'
uv build
git diff --check
```

以上也是 CI 门禁。普通测试不下载底座；Mac 装有 MLX 时会额外运行小型真实梯度和权重完整性回归。涉及模型路径、训练或缓存行为的改动，需要按 [验证指南](docs/validation.md) 补实际模型测试和相应 HTTP 验证。跳过的测试必须明确写出，不以模拟后端通过代替设备验收。

修改性能测量代码时保留原计时范围，实际运行小规模实验确认所有策略和报告字段。发布速度结论需要完整实验：同硬件、同精度、实际命中计数、预热策略、重复测量及结果差异。训练头与快捷模式、Busbar 与其他项目的可比范围须单独说明。

每次提交范围明确、可回滚，描述用户会观察到的变化、验证结果和未覆盖边界。不要重写历史 benchmark 结果；新版本建立新记录。包版本只在 `pyproject.toml` 声明，运行时和 OpenAPI 从已安装包元数据读取，避免多个版本常量漂移。
